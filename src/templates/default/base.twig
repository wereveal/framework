<!DOCTYPE html>
<!--[if lt IE 7 ]><html class="ie ie6" lang="{{ lang }}"> <![endif]-->
<!--[if IE 7 ]><html class="ie ie7" lang="{{ lang }}"> <![endif]-->
<!--[if IE 8 ]><html class="ie ie8" lang="{{ lang }}"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--><html lang="{{ lang }}"> <!--<![endif]-->
    <head>
        <meta charset="{{ charset }}" />
        {% block title %}<title>{{ title }}</title>{% endblock %}
        {% block description %}<meta name="description" content="{{ description }}">{% endblock %}
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        {% block meta %}{% endblock %}
        <base href="{{ base_url }}">

        {% block vendor_stylesheets %}
        <link rel="stylesheet" href="{{ public_dir }}/assets/vendor/bootstrap/dist/css/bootstrap.min.css">
        <script src="https://use.fontawesome.com/1465136e8a.js"></script>
        <link href='https://fonts.googleapis.com/css?family=Lato:400,400italic|Merriweather:400,400italic' rel='stylesheet' type='text/css'>
        {% endblock %}
        {% block stylesheets %}
        <link rel="stylesheet" href="{{ public_dir }}/assets/css/styles.css" media="screen">
        {% endblock %}

        <!--[if lt IE 9]>
        <script src="{{ public_dir }}/assets/vendor/html5shiv/dist/html5shiv.min.js"></script>
        <![endif]-->

        {% block icons %}
        <link rel="apple-touch-icon" sizes="180x180" href="/assets/images/apple-touch-icon.png">
        <link rel="icon" type="image/png" href="/assets/images/favicon-32x32.png" sizes="32x32">
        <link rel="icon" type="image/png" href="/assets/images/favicon-16x16.png" sizes="16x16">
        <link rel="manifest" href="/assets/images/manifest.json">
        <link rel="mask-icon" href="/assets/images/safari-pinned-tab.svg" color="#5bbad5">
        <link rel="shortcut icon" href="/assets/images/favicon.ico">
        <meta name="msapplication-config" content="/assets/images/browserconfig.xml">
        {% endblock %}

        {% block vendor_js %}
        <script src="{{ public_dir }}/assets/vendor/jquery/dist/jquery.min.js"></script>
        {% endblock %}
        {% block head_js %}{% endblock %}
    </head>
    <body>
        <div class="container container-main">
        {% block header %}
            <nav class="navbar navbar-toggleable-sm navbar-dark bg-gray" role="navigation">
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mainNav">
                    <span class="fa fa-bars"></span>
                </button>
                <a class="navbar-brand" href="#"><span class="fa fa-book slate"></span></a>
                <div class="collapse navbar-collapse" id="mainNav">
                    <ul class="navbar-nav mr-auto">
                        {% for menu in a_menus %}
                            {% if menu.css != '' %}
                                {% set extra_css %}
                                    {{ ' ' ~ menu.css }}
                                {% endset %}
                            {% else %}
                                {% set extra_css = '' %}
                            {% endif %}
                            {% if menu.url == page_url %}
                                {% set active_url = ' active' %}
                            {% else %}
                                {% set active_url = '' %}
                            {% endif %}
                            {% if menu.submenu is empty %}
                                <li class="nav-item">
                                    <a class="nav-link{{ extra_css }}{{ active_url }}" href="{{ public_dir }}{{ menu.url }}">{{ menu.text }}</a>
                                </li>
                            {% else %}
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle{{ menu.css }}{{ active_url }}" data-toggle="dropdown" href="{{ public_dir }}{{ menu.url }}" role="button" aria-haspopup="true" aria-expanded="false">{{ menu.text }}</a>
                                    <div class="dropdown-menu">
                                        {% for child in menu.submenu %}
                                            {% if child.css != '' %}
                                                {% set extra_css %}
                                                    {{ ' ' ~ child.css }}
                                                {% endset %}
                                            {% else %}
                                                {% set extra_css = '' %}
                                            {% endif %}
                                            {% if child.url == page_url %}
                                                {% set active_url = ' active' %}
                                            {% else %}
                                                {% set active_url = '' %}
                                            {% endif %}
                                            <a class="dropdown-item{{ extra_css }}{{ active_url }}" href="{{ public_dir }}{{ child.url }}">{{ child.text }}</a>
                                        {% endfor %}
                                    </div>
                                </li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </div>
            </nav>
        {% endblock %}
        {% block search %}{% endblock %}
        {% block body %}
            <div class="container">
                {{ body_text }}
            </div>
        {% endblock %}
        {% block footer %}
            <!-- Footer -->
            <div class="container footer">
                <p>Copyright &copy;&nbsp;{{ copyright_date }} <strong><a href="{{ site_url }}">{{ rights_holder }}</a></strong>, All Rights Reserved &reg;</p>
            </div>
            <!-- /footer -->
        {% endblock %}
        </div>

        {% block bottom_vendor_js %}
        <script src="{{ public_dir }}/assets/vendor/tether/dist/js/tether.min.js"></script>
        <script src="{{ public_dir }}/assets/vendor/bootstrap/dist/js/bootstrap.min.js"></script>
        {% endblock %}
        {% block bottom_js %}{% endblock %}
    </body>
</html>
{# expected array [
    'lang',
    'charset',
    'title',
    'description',
    'base_url',
    'page_url',
    'public_dir',
    'twig_prefix',
    'a_menus'
    'body_text',
    'copyright_date',
    'site_url',
    'rights_holder',
    ]
#}
